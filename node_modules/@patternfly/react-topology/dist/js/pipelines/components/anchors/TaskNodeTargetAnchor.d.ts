import { Point } from '../../../geom';
import { AbstractAnchor } from '../../../anchors';
import { Node, ScaleDetailsLevel } from '../../../types';
export default class TaskNodeTargetAnchor<E extends Node = Node> extends AbstractAnchor {
    private whenOffset;
    private detailsLevel;
    private lowDetailsStatusIconSize;
    private vertical;
    constructor(owner: E, whenOffset: number, detailsLevel?: ScaleDetailsLevel, lowDetailsStatusIconSize?: number, vertical?: boolean);
    getLocation(): Point;
    getReferencePoint(): Point;
}
//# sourceMappingURL=TaskNodeTargetAnchor.d.ts.map